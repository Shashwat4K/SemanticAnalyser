# Basic Semantic Analysis with lex and yacc

Verifies if variables are declared before operation.
Verifies whether variables in operation are type compatible.

Assuming no loops, switch statements, no global variables are there and only function is main().
Assignments are of the form a = b; or a = b 'op' c;
